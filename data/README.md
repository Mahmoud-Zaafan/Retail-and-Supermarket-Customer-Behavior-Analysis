# Data Folder

This directory stores the datasets used in the project.

- `raw/` – place the original transaction dataset `customer_supermarket.csv` here. This file is not tracked in the repository.
- `processed/` – contains the cleaned datasets generated after running the preprocessing notebook.

The preprocessing notebook in `../notebooks/` reads the raw file and outputs new CSV files in the `processed/` subdirectory.
